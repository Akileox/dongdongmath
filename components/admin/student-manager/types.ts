export type Student = {
    id: string
    full_name: string
    phone: string
    grade: string
    school: string
    role: string
    class_section?: string
    current_study?: string
    email?: string
    name?: string // sometimes used as fallback
}
